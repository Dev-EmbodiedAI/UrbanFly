# 安全策略

## 密钥

确定性仿真不需要 Qwen Key。可选 Qwen 功能从进程环境读取 `DASHSCOPE_API_KEY` 或 `URBANFLY_QWEN_API_KEY`。不要把密钥写入 `.env.example`、JSON 报告、命令历史、Issue、视频或截图；一旦泄漏应立即轮换。

## 本地服务边界

打包版控制服务只绑定 `127.0.0.1:8765`。在没有认证和 TLS 的情况下，不要把它暴露给不可信网络。Qwen 只拥有语义提议权限，其输出必须通过确定性后端安全门；模型不能直接发送电机、姿态、速度或航点控制命令。

## 漏洞报告

发现漏洞时请创建私有 GitHub Security Advisory，并提供最小复现。不要附带 API Key、专有数据或原始个人飞行日志。
