# UrbanFly Backend Dependencies
# =============================

# Core
numpy>=2.0.0
scipy>=1.10.0

# Mesh Processing (Phase 2: OBJ preprocessing)
trimesh>=4.0.0
scikit-image>=0.25.2

# Web Server (Phase 4: WebSocket + HTTP)
aiohttp>=3.9.0

# Optional: faster OBJ parsing
# pywavefront>=1.3.0

# Optional: advanced mesh processing
# open3d>=0.18.0

# Optional: texture processing
Pillow>=10.0.0

# VLN / risk world model
torch>=2.1.0
matplotlib>=3.7.0
pytest>=7.0.0

# Published Qwen integration uses the OpenAI-compatible API and Python's
# standard HTTP stack. Local checkpoint reproduction is optional and isolated
# in requirements-local-qwen.txt; raw Qwen weights are never distributed.
